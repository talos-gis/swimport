# pragma once

int sum(int const * A3_members, size_t n1, size_t n2, size_t n3);
int product(int const * AIO_members, size_t n1);
void mul(int const * A2_a, size_t m_a, size_t n_a, int const * A2_b, size_t m_b, size_t n_b, int *& AF2_c, size_t& m_c, size_t& n_c);